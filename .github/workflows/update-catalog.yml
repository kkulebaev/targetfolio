name: Update catalog

on:
  schedule:
    - cron: "0 16 * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      # invest-public-api.tinkoff.ru отдаёт цепочку от Russian Trusted Root CA,
      # которого нет в trust store ubuntu-раннера — без этого fetch падает
      # с SELF_SIGNED_CERT_IN_CHAIN.
      - run: pnpm catalog:fetch
        env:
          TINKOFF_TOKEN: ${{ secrets.TINKOFF_TOKEN }}
          NODE_EXTRA_CA_CERTS: ${{ github.workspace }}/certs/russian-trusted-root-ca.pem

      - name: Commit and push if changed
        run: |
          if git diff --quiet src/catalog/instruments.json; then
            echo "No changes in catalog"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/catalog/instruments.json
          git commit -m "chore(catalog): refresh instruments and close prices"
          git push
